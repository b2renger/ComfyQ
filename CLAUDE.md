# ComfyQ — project memory

Portable project knowledge for ComfyQ (multi-user ComfyUI workflow scheduler). The **v2 rebuild is now the active code on the default `main` branch** — the standalone `v2` branch is a stale snapshot (behind `main`), and the original v1 monolith lives on `poc1` / `poc2`. So: work on `main`; ignore any older doc text that says "checkout v2". This file lives in the repo so it travels with the project across machines/drives — keep it here, not in any machine-local `~/.claude/` folder. Paths below are **relative to the repo root** (the drive letter changes between machines, so never hardcode `D:\`).

Detailed, evolving plans live in [implementation_plan.md](implementation_plan.md); user-facing overview in [README.md](README.md). This file is the durable, auto-loaded summary + the "why" behind decisions that aren't obvious from the code.

## Latest progress (2026-06-15)

**LivePortrait portrait-animation registered + verified on rig (2026-06-15)** — see the off-queue entry in the roadmap below. First workflow combining an **image INPUT + a video INPUT**, and the first to use **`VHS_VideoCombine`** as its output node; zero new code.

**All target workflows now registered and bundled:**
- **TripoSplat** (image→3D Gaussian splat + mesh) ✅ verified on rig
- **Qwen multi-angle** (1 image → 8 views) ✅ with N-image gallery
- **Stable Audio 3** (text → audio) ✅ with audio player + subfolder-serving fix
- **Ideogram 4.0** (structured JSON t2i) ✅ zero-code
- **Wan 360° LoRA** (image → 360 video) ✅ zero-code
- **LTX FLF2V** (first+last frame → video+audio) ✅ first multi-image-INPUT
- **LTX IC-LoRA vid2vid** (video + MoGe depth → restyle+audio) ✅ first video-INPUT; added `disabledWhen` for field gating
- **LTX ID-LoRA img+audio2vid** (image + audio → lip-synced video) ✅ first audio-INPUT; completed audio I/O

**Cross-cutting infra built:** Gaussian-splat viewer (Spark), N-image gallery, audio player, audio upload widget, subfolder media serving, param field gating (`disabledWhen`). Multi-output UX is complete; audio I/O is complete; 3D viewers are complete.

## Runtime / environment
- **Node: LTS only.** Currently **Node 24 "Krypton"**, pinned in [.nvmrc](.nvmrc) (`24`) with `engines.node` = `>=22.0.0 <25.0.0` in the root, [server](server/package.json), and [client](client/package.json) `package.json`. Odd-numbered "Current" releases (23, 25, …) are unsupported — the lab runs varied machines and needs an LTS line. When Node 26 becomes LTS (~Oct 2026), bump `.nvmrc` + the three `engines` ceilings.
- **Windows winget gotcha:** winget ships Node as two packages. `OpenJS.NodeJS` is the **wrong** (Current) one; use **`OpenJS.NodeJS.LTS`**. To swap: `winget uninstall --id OpenJS.NodeJS --exact` then `winget install --id OpenJS.NodeJS.LTS --exact` (needs admin; globals in `%AppData%\npm` survive).
- **`better-sqlite3` is the only native dep**, pinned to v12+ for N-API prebuilds (one binary across Node versions, no per-machine recompile). After any Node *major* change, wipe `node_modules` and `npm install` so the ABI matches.

## Serving model — plain HTTP (do NOT re-add HTTPS)
- The dev/workshop UI is served over **plain HTTP** (Vite on :5173, proxying the Express backend). The self-signed HTTPS layer (`@vitejs/plugin-basic-ssl`) was tried and **reverted 2026-05-19**: self-signed certs can't be trusted across BYOD Safari + Chrome + mobile without installing a CA on every device, and Safari refused to extend a click-through to the websocket and to media downloads.
- **Don't reintroduce `@vitejs/plugin-basic-ssl` or `https: true`** in [client/vite.config.js](client/vite.config.js). The only feature it enabled — in-browser webcam capture (`getUserMedia`, needs a secure context) — was removed; image/video inputs are uploaded as files (a phone's file picker still offers "Take Photo").

## Workflow roadmap (target-workflows queue, COMPLETE 2026-06-13)
Each row is a probe into "does ComfyQ stay zero-config when a new workflow lands?" — fixes land generally (registry / primitive-fallback parser / MediaStore classifier / viewers / multi-output UX), never per-workflow. **All active targets registered; rig smoke-testing pending.**
1. **TripoSplat** (image → Gaussian splat) — replaces the dropped Hunyuan3D row. **DONE — verified on rig 2026-06-10** (distinct `.spz`/`.ply`/`.glb` confirmed landing via `SaveGLB`; viewers + toggle confirmed working). Registered at [workflows/3d_triposplat_image_to_gaussian_splat/](workflows/3d_triposplat_image_to_gaussian_splat/) — video chain dropped (not wanted), a `.ply` splat export added so the three exports are `.spz`/`.ply`/`.glb`. **Gaussian-splat viewer** ([client/src/components/ui/SplatViewer.jsx](client/src/components/ui/SplatViewer.jsx), Spark `@sparkjsdev/spark`) uses a **continuous `setAnimationLoop`** (NOT ModelViewer's render-on-demand — Spark's async splat re-sort needs a steady loop, else wheel-zoom/right-click-pan look dead; safe because it only mounts in the lightbox, one instance). GLB meshes → [ModelViewer.jsx](client/src/components/ui/ModelViewer.jsx). `ImageLightbox` 3D gallery = Splat⇄Mesh toggle + 3 export buttons; `splat` media kind in [server/media/mediaTypes.js](server/media/mediaTypes.js). User-facing **"Auto-remove background"** toggle exposed via node `88:35.switch` (checkbox param) — drove the first `checkbox` param renderer in [BookingDialog.jsx](client/src/components/BookingDialog.jsx).
2. **Qwen image-to-multiview (multiple scene angles)** — **registered 2026-06-11; pending rig smoke-test.** At [workflows/image_qwen_multiple_angles/](workflows/image_qwen_multiple_angles/). One scene image → 8 angle images (Qwen-Edit 2509 + multiple-angles/Lightning LoRAs); 1-click (only the scene image is exposed). Pruned 8 `PreviewImage` temp-dupes. Built the **N-image gallery** ([client/src/components/ui/ImageGallery.jsx](client/src/components/ui/ImageGallery.jsx): main+thumbnails+counter+labels+download-all), wired into `ImageLightbox` for any >1-image job, + a "{N} views" badge on Scheduler cards. **Multi-output UX is now DONE** (3D gallery + N-image gallery + audio player).
3. **Audio — Stable Audio 3 Medium (text→audio)** — **registered 2026-06-11; pending rig smoke-test.** At [workflows/audio_stable_audio_3_medium/](workflows/audio_stable_audio_3_medium/). In-graph automated magic-prompt LLM (`TextGenerate` qwen3.5; `ComfySwitchNode` toggles raw vs LLM-expanded per category). Built the **audio player** ([client/src/components/ui/AudioPlayer.jsx](client/src/components/ui/AudioPlayer.jsx), `isAudio` → styled `<audio>` in cards + lightbox gallery). **General subfolder-serving fix** in [server/executor/jobExecutor.js](server/executor/jobExecutor.js): folds ComfyUI's subfolder into the wire `filename` (output-root-relative) so `audio/…` / `3d/…` saves serve via `/images/:filename(*)` instead of 404-ing on basename. Audio **input** still missing (not needed for text→audio; revisit for ACE).
4. **Ideogram 4.0 t2i** — **registered 2026-06-10; pending rig smoke-test.** At [workflows/image_ideogram4_t2i/](workflows/image_ideogram4_t2i/). The export had a generator island (hardcoded structured-JSON prompt in `CLIPTextEncode` 98:24) + a *disconnected* magic-prompt builder (`134:*` → `PreviewAny`, manual LLM round-trip, doesn't touch the image). Per owner's call we shipped the **single-run structured-JSON path**: pruned the builder/PreviewAny island, exposed 98:24's JSON as the prompt + aspect ratio / megapixels / quality preset (Turbo/Default/Quality) / CFG / seed. Zero new code (standard t2i). **Rig check:** `aspect_ratio` combo options are best-guess; default `"9:16 (Portrait Widescreen)"` is known-good, verify the rest in the editor.

Also shipped (zero-code, off-queue): **Wan 2.1 360° rotate LoRA i2v** ([workflows/video_wan_rotate_lora/](workflows/video_wan_rotate_lora/)) — the "360 video LoRA" target; reused the LTX i2v image→video infra. Prompt carries the LoRA trigger `r0t4tion` + "360 degrees rotation" (exposed as-is). **LTX 2.3 FLF2V with audio** ([workflows/video_ltx2_3_flf2v/](workflows/video_ltx2_3_flf2v/)) — first+last frame → mp4 with an embedded audio track (covers the LTX video-from-reference + audio-driven backlog); first multi-image-INPUT workflow (2× image params), zero new code. **LTX 2.3 IC-LoRA vid2vid** ([workflows/video_ltx2_3_ic_lora_vid2vid/](workflows/video_ltx2_3_ic_lora_vid2vid/)) — source video → MoGe depth → IC-LoRA restyle + audio (covers the depth-preprocessor target); **first video-INPUT workflow** (`LoadVideo` upload path confirmed). Introduced the generic **`disabledWhen`** param field (`{param,equals}` in [schemas.js](server/config/schemas.js)) → greys a field in BookingDialog while another param holds a value (used for its two either/or prompt boxes gated by an Enhance toggle). Caveats: video input has no calibration auto-sample (calibrate via a real run); WorkflowMetaEditor has no `disabledWhen` UI yet (admin re-save drops it). **LTX 2.3 ID-LoRA img+audio→talking video** ([workflows/video_ltx2_3_id_lora_img_audio2vid/](workflows/video_ltx2_3_id_lora_img_audio2vid/)) — character image + reference audio → lip-synced talking video; **first audio-INPUT workflow**, which completed **audio I/O = DONE**: added an `audio` type to [MediaCaptureField.jsx](client/src/components/capture/MediaCaptureField.jsx) (file picker + drag-drop + inline `<audio>` preview, no resize) + `BookingDialog` routing; server needed zero changes (LoadAudio gets the uploaded filename via the generic materializer). **LivePortrait portrait-animation** ([workflows/video_liveportrait_image2video/](workflows/video_liveportrait_image2video/)) — portrait image + driving face video → animated (silent) talking-head video (LivePortraitKJ: head pose + eye/lip motion transferred onto the still). **Verified on rig 2026-06-15.** First workflow combining an **image INPUT + a video INPUT** (the generic materializer injects both `196.image` and `8.video` with no new code), and the first to use **`VHS_VideoCombine`** as the output node instead of the LTX family's core `SaveVideo` — needed only `save_output: true` so the result lands in the durable output dir rather than ComfyUI `temp/` (which is purged on restart); the generic output collector walks VHS's `gifs` key and the `filename_prefix` auto-injection namespaces it, so **zero server changes**. The raw export carried a dead branch that I pruned to the 14-node live path: removed a `MaskPreview+` (an OUTPUT_NODE → would have dropped a stray mask image into the gallery), a disconnected `LivePortraitRetargeting` + duplicate driving-video `LivePortraitCropper` pair, the unused InsightFace cropper loader (only the MediaPipe cropper is wired), and a size-tap. Exposes portrait image / driving video / max driving frames (caps runtime + output length). **Calibration caveat:** both inputs must contain a *face* (the generic asset resolver can't guarantee one) and LivePortrait is deterministic (no seed → ComfyUI cache returns ~1s on re-calibration), so calibrate via a real booked run, not the auto-gauge. Backlog/superseded: Hunyuan3D 2.1. Full detail + the cross-cutting infra state (GS viewer = BUILT, multi-output UX = DONE/3D+N-image galleries, audio I/O = DONE/player+input; long-job robustness = READY) is in [implementation_plan.md](implementation_plan.md) under "Target workflows".

## Key implementation patterns

**Workflow registration:** Each workflow is a folder bundle: `workflows/<id>/<id>.api.json` (the ComfyUI API-format export) + `<id>.meta.json` (parameters, description, category). The `meta.json` is read-only; admins edit via UI (writes to `.config.meta.json` override file). This keeps the export distinct from deployment tweaks.

**Output detection (generic):** No `class_type` whitelist. [server/media/mediaTypes.js](server/media/mediaTypes.js) classifies by file extension: `.glb/.gltf` → model3d, `.spz/.splat/.ksplat` → splat, `.mp3/.wav/…` → audio, `.mp4/.webm/…` → video, else image. New output formats Just Work™ — no code changes needed.

**Subfolder serving:** ComfyUI save nodes write to subfolders (e.g., `SaveAudioMP3` → `audio/track.mp3`). The wire carries the full output-root-relative filename; [server/executor/jobExecutor.js](server/executor/jobExecutor.js) folds the subfolder into `filename` and clears the `subfolder` field so `/images/:filename(*)` serves them without breaking existing routes.

**Multi-output galleries:** When a job produces >1 image, the lightbox renders [ImageGallery.jsx](client/src/components/ui/ImageGallery.jsx) (main image + thumbnails + prev/next). When it has both a splat and a mesh, a toggle switches between the 3D viewers. Audio outputs render the AudioPlayer. The Scheduler grid shows a "{N} views" badge for multi-output jobs.

**Param field gating (`disabledWhen`):** A parameter can declare `disabledWhen: { param, equals }` in the meta to grey itself out when another param holds a value. Used for either/or prompts (raw vs. LLM-enhanced). The field still submits; the workflow's switch node ignores the unused branch.

**Calibration (auto, single run split into load+gen, admin-mode):** [server/benchmark/benchmarkService.js](server/benchmark/benchmarkService.js) runs the workflow **once** (explicit `/free` first so models load) and splits the run at the first sampler `progress` event: start→first-step = `modelLoadSec`, first-step→end = `estimatedDurationSec`/`warmDurationSec` (the recurring/optimal cost the timeline uses); full wall time = `coldDurationSec` (first-run incl. load); plus `samplesPerSec` + `gpu`. **Do NOT add a second "warm" run** — ComfyUI caches identical graphs and returns the cached result in ~1s, which made calibration report 1s for a 25s job (regression fixed 2026-06-13). Instead `_randomizeSeeds()` bumps every literal `*seed` node field **and** `_buildCalibrationParams` randomizes every exposed seed param, so the single run always actually executes (cache-immune, even on re-calibration). Inputs **auto-resolved from `config.assets.dir`** (`D:\_assets`): `_resolveAsset(type)` picks a median image / smallest video / audio clip, staged via the worker's `InputUploader` (image falls back to a built-in PNG; missing video/audio asset → clear error). Gauge button works in **admin mode** via [server/benchmark/adminCalibrator.js](server/benchmark/adminCalibrator.js) (lazily spawns/attaches ComfyUI, reuses it, idle-frees VRAM, leaves ComfyUI running on shutdown). **No UI, no uploads.** Student-mode caveat: calibrate while the queue is idle (executor + calibration share the one worker).

**Image upload guard:** Three-layer protection against OOM crashes from oversized images: (1) client throws `ImageProcessError` on HEIC/decode failure; (2) [MediaCaptureField.jsx](client/src/components/capture/MediaCaptureField.jsx) shows inline error; (3) [server/routes/uploads.js](server/routes/uploads.js) backstop rejects HEIC (extension + ftyp magic byte) and images not fitting 1920×1080 box in either orientation. Server uses `image-size` library (pure JS, no native dep).

**Prompt display resilience:** `pickHeadlinePrompt()` in [BookingDialog.jsx](client/src/components/BookingDialog.jsx) finds the user's text at submit time (prefers textarea params, skips negative-keyed ones). `getDisplayPrompt()` in [jobDisplay.js](client/src/utils/jobDisplay.js) mines `job.params` at render time so historical jobs display even if exposed under non-`prompt` keys (e.g., LTX `positive_prompt`, Ideogram `description`).

## Federation (Phase F — deferred, design locked)
The final v2 phase is multi-instance LAN federation (mDNS peers, fleet admin view, student station picker, orchestrator role). Design is locked, implementation deferred; full plan in [implementation_plan.md](implementation_plan.md) → "Phase F". **Implication for current work:** don't bake in single-instance assumptions — if a feature surfaces user-visible state that should be aggregable across peers later (e.g. job history), structure it so a future `/federation/*` endpoint can return it without a schema migration.

## Conventions
- **API-format workflows only** (no Litegraph auto-conversion). **Generic output detection** by file extension, not `class_type`.
- Commit/push only when the user explicitly asks.
